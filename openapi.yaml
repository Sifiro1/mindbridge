openapi: 3.0.0
info:
  title: MindBridge API
  version: 1.0.0
paths:
  /api/v1/play/submit:
    post:
      summary: Submit a completed task
      responses:
        '200':
          description: Task submission received
